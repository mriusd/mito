/**
 * Single source of truth for live layout persistence + mito-layout file exports.
 * Keep import/apply stamping this value so checkLayoutVersion never wipes a fresh import.
 */
export const LAYOUT_VERSION = 10;
