/** @deprecated — use uiInteractionQuiet (kept for existing imports). */
export {
  noteUiScrollActivity,
  noteUiInteractionActivity,
  isUiScrollQuiet,
  isUiInteractionQuiet,
  installUiScrollQuietListeners,
  installUiInteractionQuietListeners,
} from './uiInteractionQuiet';
